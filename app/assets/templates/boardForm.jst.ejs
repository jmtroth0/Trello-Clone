<h1><%= board.id ? "Edit Board" : "New Board" %></h1>

<p class='errors'></p>

<form>
  <label for="title">Title</label>
  <input
    id="title"
    type="text"
    name="title"
    value="<%= board.escape("title") %>">

  <input
    type="submit"
    value="submit">
</form>
